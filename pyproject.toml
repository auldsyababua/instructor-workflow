[project]
name = "instructor-workflow"
version = "0.1.0"
description = "Workflow project using instructor"
requires-python = ">=3.9"
dependencies = [
    "instructor",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
